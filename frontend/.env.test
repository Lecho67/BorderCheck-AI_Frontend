# Valores dummy para que el entorno de tests sea determinístico y no dependa
# del .env local (gitignored). No son secretos. Vitest carga este archivo
# automáticamente en modo test.
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
VITE_API_BASE_URL=http://localhost:3000
